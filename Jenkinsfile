pipeline {
	
	agent { label 'maven-jdk-8' }
	
	stages {
		stage ('Compile Stage') {
			
			steps {
				sh 'mvn --version'
			}
		}
		
		stage ('Testing Stage') {
			steps {
				sh 'mvn --version'
			}
		}
		
		stage ('Deployment Stage') {
			steps {
				sh 'mvn --version'
			}
		}
	}

}
